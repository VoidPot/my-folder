---
const { htmlString, value, isTextRight = false } = Astro.props;
---

<div
  class:list={[
    "flex flex-col gap-2",
    {
      "text-left": !isTextRight,
      "text-right": isTextRight,
    },
  ]}
>
  <p
    class="uppercase font-sans text-xs font-semibold text-blue-400"
    set:html={htmlString}
  />
  <span
    class="font-mono text-xs font-medium text-gray-600 leading-7"
    set:html={value}
  />
</div>
